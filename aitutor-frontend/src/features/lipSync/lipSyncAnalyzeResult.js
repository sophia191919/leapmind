/**
 * LipSyncAnalyzeResult 类型（JS 版本，供编辑器智能提示与文档使用）
 *
 * @typedef {Object} LipSyncAnalyzeResult
 * @property {number} volume - 归一化音量，范围约为 [0, 1]
 */

// 这是一个仅包含类型定义的模块，运行时不导出任何内容
// 若需要从该文件中 import type，可继续保留本文件；若不需要可删除。
export {};