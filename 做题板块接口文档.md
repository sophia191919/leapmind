# LeapMind 做题板块接口文档

> 文档版本：2026-08-04
>
> 对应分支：`frontend`（做题模块前端）、`backend-M1`（做题模块后端）、`develop`（M1 后端联调代码）
>
> 后端开发地址：`http://127.0.0.1:8080`
>
> 前端开发地址：`http://127.0.0.1:5173`

## 1. 联调约定

### 1.1 鉴权

除登录接口外，本文中的做题接口均需要 JWT：

```http
Authorization: Bearer <token>
Content-Type: application/json
Accept: application/json
```

登录获取 Token：

```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "xiaoming1",
  "password": "123456"
}
```

成功响应示例：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJ...",
    "userInfo": {
      "id": 1,
      "username": "xiaoming1",
      "identify": "student",
      "studentName": "小明1"
    },
    "expiresIn": 86400000
  },
  "timestamp": 1785859200000
}
```

### 1.2 统一 JSON 响应

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": 1785859200000
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | number | 业务状态码，成功为 `200` |
| `message` | string | 中文提示信息 |
| `data` | object/array/null | 实际业务数据 |
| `timestamp` | number | 服务端毫秒时间戳 |

错误示例：

```json
{
  "code": 401,
  "message": "未认证",
  "data": null,
  "timestamp": 1785859200000
}
```

### 1.3 枚举值

| 类型 | 可用值 |
| --- | --- |
| 题型 `questionType` | `SINGLE_CHOICE`、`MULTIPLE_CHOICE`、`FILL_BLANK`、`SHORT_ANSWER` |
| 难度 `difficulty` | `BASIC`、`ADVANCED`、`HARD` |
| 题目状态 `status` | `ENABLED`、`DISABLED`；删除已作答题目后内部状态为 `ARCHIVED` |
| 做题模式 `mode` | `SEQUENTIAL`、`RANDOM`、`MISTAKES`、`FREE_PRACTICE`、`AFTER_CLASS`、`MISTAKE_REDO` |
| 错题状态 | `UNRESOLVED`、`REVIEWING`、`RESOLVED` |
| 统计范围 `range` | `today`、`week`、`month`、`all` |

---

## 2. 接口总览

| 模块 | 方法 | 路径 | 说明 |
| --- | --- | --- | --- |
| 筛选 | GET | `/api/practice/filters` | 获取科目、知识点、题型等筛选项 |
| 题库 | GET | `/api/practice/questions` | 分页查询题目 |
| 题库 | GET | `/api/practice/questions/{questionId}` | 查询题目详情 |
| 题库 | POST | `/api/practice/questions` | 新增题目 |
| 题库 | PUT | `/api/practice/questions/{questionId}` | 修改题目 |
| 题库 | DELETE | `/api/practice/questions/{questionId}` | 删除/归档题目 |
| 导入 | POST | `/api/practice/questions/import` | Excel、CSV、Word、PDF 导入 |
| 导入 | GET | `/api/practice/questions/import-template` | 下载 Excel 模板 |
| 做题 | GET | `/api/practice/next` | 按模式获取下一题 |
| 做题 | POST | `/api/practice/submit` | 提交答案并持久化 |
| 推荐 | GET | `/api/practice/recommendations` | 获取当前用户的薄弱知识点练习推荐 |
| 学情 | POST | `/api/practice/sessions/complete` | 向 M6 上报练习会话完成事件 |
| 记录 | GET | `/api/practice/records` | 查询个人答题记录 |
| 错题 | GET | `/api/practice/mistakes` | 查询错题列表（兼容接口） |
| 错题 | PATCH | `/api/practice/mistake-book/{mistakeId}` | 修改错题状态/笔记 |
| 错题 | PATCH | `/api/practice/mistakes/{recordId}` | 从答题记录修改错题信息 |
| 错题 | GET | `/api/wrong-questions` | 查询错题本 |
| 错题 | PUT | `/api/wrong-questions/{id}/focus` | 收藏或取消收藏 |
| 错题 | DELETE | `/api/wrong-questions/{id}` | 删除错题记录 |
| 错题 | POST | `/api/wrong-questions/batch-redo` | 生成错题重做会话 |
| 看板 | GET | `/api/practice/dashboard` | 积分、连续天数、今日进度 |
| 统计 | GET | `/api/practice/statistics` | 获取细化刷题分析 |
| AI 记忆 | GET | `/api/practice/review-reminders` | 获取当前用户到期复习提醒 |
| AI 记忆 | POST | `/api/practice/review-reminders/{reminderId}/complete` | 完成复习提醒 |
| 排行榜 | GET | `/api/practice/leaderboards` | 获取方向榜单 |
| 签到 | POST | `/api/practice/checkin` | 每日签到 |
| 签到 | GET | `/api/practice/checkin/status` | 查询签到状态 |
| 隐私 | PATCH | `/api/practice/privacy` | 隐藏/显示排行榜身份 |
| 导出 | GET | `/api/practice/export` | 导出答题记录文本 |
| 清理 | DELETE | `/api/practice/records` | 清空当前用户刷题数据 |

---

## 3. 题库接口

### 3.1 获取筛选项

```http
GET /api/practice/filters
```

`data` 示例：

```json
{
  "subjects": ["数学", "英语", "计算机"],
  "gradeLevels": ["大学"],
  "tracks": ["高数期末"],
  "chapters": ["函数极限"],
  "knowledgePoints": ["重要极限"],
  "questionTypes": ["SINGLE_CHOICE", "MULTIPLE_CHOICE", "FILL_BLANK", "SHORT_ANSWER"],
  "difficulties": ["BASIC", "ADVANCED", "HARD"],
  "modes": ["FREE_PRACTICE", "AFTER_CLASS", "MISTAKE_REDO", "SEQUENTIAL", "RANDOM", "MISTAKES"],
  "mistakeStatuses": ["UNRESOLVED", "REVIEWING", "RESOLVED"]
}
```

### 3.2 分页查询题库

```http
GET /api/practice/questions?page=1&pageSize=12&subject=数学&status=ENABLED&keyword=导数
```

查询参数均为可选：

| 参数 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `page` | number | `1` | 页码 |
| `pageSize` | number | `12` | 每页条数，最大 `100` |
| `subject` | string | - | 科目 |
| `gradeLevel` | string | - | 年级/学段 |
| `track` | string | - | 题库方向 |
| `chapter` | string | - | 章节 |
| `knowledgePoint` | string | - | 知识点 |
| `questionType` | string | - | 题型枚举 |
| `difficulty` | string | - | 难度枚举 |
| `lessonId` | string | - | 课时 ID |
| `status` | string | 非归档题 | 题目状态 |
| `keyword` | string | - | 模糊搜索标题、题干、知识点 |

`data` 示例：

```json
{
  "records": [
    {
      "id": 12,
      "subject": "数学",
      "gradeLevel": "大学",
      "questionType": "SINGLE_CHOICE",
      "title": "导数计算",
      "content": "函数 f(x)=x^2 在 x=2 处的导数是多少？",
      "options": {"A": "2", "B": "4", "C": "6", "D": "8"},
      "correctAnswer": "B",
      "answerKeywords": "",
      "analysis": "f'(x)=2x，因此 f'(2)=4。",
      "chapter": "导数与微分",
      "knowledgePoint": "导数定义",
      "difficulty": "BASIC",
      "track": "高数期末",
      "lessonId": null,
      "status": "ENABLED"
    }
  ],
  "total": 15,
  "page": 1,
  "pageSize": 12
}
```

> 备注：该管理列表当前会返回 `correctAnswer`。正式生产环境如需防止提前看到答案，应拆分管理端和学生端 DTO。

### 3.3 查询题目详情

```http
GET /api/practice/questions/12
```

返回单个完整题目对象，字段同题库列表中的 `records[]`。

### 3.4 新增题目

```http
POST /api/practice/questions
Content-Type: application/json

{
  "subject": "数学",
  "gradeLevel": "大学",
  "track": "高数期末",
  "chapter": "导数与微分",
  "knowledgePoint": "导数定义",
  "questionType": "SINGLE_CHOICE",
  "difficulty": "BASIC",
  "title": "导数计算",
  "content": "函数 f(x)=x^2 在 x=2 处的导数是多少？",
  "optionA": "2",
  "optionB": "4",
  "optionC": "6",
  "optionD": "8",
  "correctAnswer": "B",
  "answerKeywords": "",
  "analysis": "f'(x)=2x，因此 f'(2)=4。",
  "lessonId": null,
  "status": "ENABLED"
}
```

必填字段：`title`、`content`、`correctAnswer`、`chapter`、`knowledgePoint`。

选择题至少填写 `optionA`、`optionB`。

默认值：

- `subject`：`通用`
- `gradeLevel`：`大学`
- `questionType`：`SINGLE_CHOICE`
- `difficulty`：`BASIC`
- `track`：默认等于科目
- `status`：`ENABLED`

### 3.5 修改题目

```http
PUT /api/practice/questions/12
Content-Type: application/json
```

请求体与新增题目一致，成功后返回更新后的完整题目对象。

### 3.6 删除题目

```http
DELETE /api/practice/questions/12
```

> 备注：没有答题记录的题目会物理删除；已经被作答过的题目不会直接删除，而会改成 `ARCHIVED`，以保留历史统计完整性。

---

## 4. 题库文件导入

### 4.1 导入题目

```http
POST /api/practice/questions/import
Content-Type: multipart/form-data

file=<题库文件>
```

支持格式：`.xlsx`、`.xls`、`.csv`、`.docx`、`.doc`、`.pdf`。

单次最多导入 `1000` 道题。

`data` 示例：

```json
{
  "filename": "数学题库.xlsx",
  "supportedFormats": ".xlsx、.xls、.csv、.docx、.doc、.pdf",
  "totalRows": 20,
  "inserted": 18,
  "pendingReview": 2,
  "failed": 0,
  "errors": []
}
```

导入字段顺序：

```text
subject, gradeLevel, track, chapter, knowledgePoint, questionType,
difficulty, title, content, optionA, optionB, optionC, optionD,
correctAnswer, answerKeywords, analysis, lessonId, status
```

也支持中文表头，例如：科目、年级、方向、章节、知识点、题型、难度、标题、题目、选项A、选项B、答案、关键词、解析、状态。

Word/PDF 文本示例：

```text
题目：函数 f(x)=x^2 在 x=2 处的导数是多少？
科目：数学
章节：导数与微分
知识点：导数定义
题型：单选
难度：基础
A：2
B：4
C：6
D：8
答案：B
解析：f'(x)=2x，因此 f'(2)=4。
```

导入规则备注：

- 缺少答案的题目会写入题库，但自动标记为 `DISABLED`，计入 `pendingReview`，需补充答案后启用。
- Word 表格优先按模板表头识别；普通 Word/PDF 文本需要“题目：”“答案：”等标签。
- PDF 只提取文本层；扫描图片型 PDF 暂不支持 OCR。
- 文件无法识别、格式不受支持或超过 1000 题时返回错误。

### 4.2 下载 Excel 模板

```http
GET /api/practice/questions/import-template
```

返回文件：`practice-question-template.xlsx`。

---

## 5. 做题与判题

### 5.1 获取下一题

```http
GET /api/practice/next?mode=RANDOM&subject=数学&questionType=SINGLE_CHOICE&difficulty=BASIC
```

可选参数：`mode`、`subject`、`gradeLevel`、`track`、`chapter`、`knowledgePoint`、`questionType`、`difficulty`、`lessonId`。

`data` 示例：

```json
{
  "question": {
    "id": 12,
    "subject": "数学",
    "questionType": "SINGLE_CHOICE",
    "title": "导数计算",
    "content": "函数 f(x)=x^2 在 x=2 处的导数是多少？",
    "options": {"A": "2", "B": "4", "C": "6", "D": "8"},
    "analysis": "f'(x)=2x，因此 f'(2)=4。",
    "chapter": "导数与微分",
    "knowledgePoint": "导数定义",
    "difficulty": "BASIC",
    "track": "高数期末",
    "status": "ENABLED"
  },
  "mode": "RANDOM",
  "stats": {}
}
```

> 此接口的 `question` 不返回 `correctAnswer`。普通模式会尽量避开用户最近 7 天做过的题；无可用新题时会回退到符合条件的旧题。

### 5.2 前端生成一组练习的说明

当前没有单独的“批量创建普通练习会话”后端接口。前端调用：

```http
GET /api/practice/questions?page=1&pageSize=100&status=ENABLED&subject=数学
```

然后在浏览器中抽取用户选择的题数：

- 题量范围：`1~50`
- 可选择单科，也可选择混合科目
- 可用题不足时按实际数量生成
- 混合模式由前端尽量均衡抽取各科目

> 联调重点：`questionCount` 目前是前端参数，不是 `/api/practice/questions` 的后端参数。

### 5.3 提交答案

```http
POST /api/practice/submit
Content-Type: application/json

{
  "questionId": 12,
  "userAnswer": "B",
  "durationSeconds": 35,
  "mode": "FREE_PRACTICE",
  "sessionId": "sess_1785859200000",
  "hintCount": 1,
  "confusionTag": "formula_confusion"
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `questionId` | number | 是 | 题目 ID |
| `userAnswer` | string | 是 | 单选如 `B`；多选可传 `A,C`；填空/简答传文本 |
| `durationSeconds` | number | 否 | 本题用时，负数会按 0 处理 |
| `mode` | string | 否 | 来源模式，默认 `SEQUENTIAL` |
| `sessionId` | string | 是 | 练习会话 ID；同一组练习必须保持一致，只能包含字母、数字、点、下划线、冒号或连字符，最长 64 位 |
| `hintCount` | number | 否 | 本题使用提示的次数，范围 `0~100`，默认 `0` |
| `confusionTag` | string | 否 | 困惑标签，可用值：`concept_unclear`、`formula_confusion`、`step_unclear`、`application_difficulty`、`careless_error` |

`data` 示例：

```json
{
  "record": {
    "id": 101,
    "questionId": 12,
    "questionTitle": "导数计算",
    "answeredAt": "2026-08-04T20:30:00",
    "durationSeconds": 35,
    "userAnswer": "B",
    "correctAnswer": "B",
    "correct": true,
    "judgeScore": 100.0,
    "judgeFeedback": "选择题精确匹配正确",
    "points": 5,
    "attemptNumber": 1,
    "sourceMode": "FREE_PRACTICE"
  },
  "question": {},
  "correct": true,
  "judgeScore": 100.0,
  "judgeFeedback": "选择题精确匹配正确",
  "points": 5,
  "dailyBonus": 0,
  "attemptNumber": 1,
  "conquered": false,
  "surpassPercent": 60.0,
  "nextQuestion": {},
  "dashboard": {}
}
```

判题说明：

- 单选题：选项精确匹配。
- 多选题：忽略选项顺序后比较，例如 `A,C` 与 `CA` 可视为同一集合。
- 填空题：支持标准答案/关键词、数值误差 `0.0001` 以内以及相似度匹配。
- 简答题：核心关键词覆盖率达到 `60%` 判定通过；AI 可用时追加语义反馈，不可用时使用本地关键词策略。
- 答错后自动新增或更新错题本；之后答对会将对应错题改为 `RESOLVED`。
- 首次答对按难度给 `5/10/15` 分，第二次答对减半，第三次及以后不再给本题积分。

跨模块事件说明：

- 每次成功提交答案后，M1 会保留原有通用事件记录，固定为 `module=M1`、`eventType=EXERCISE_SUBMITTED`、`processed=0`。
- 同时以当前 JWT 用户身份向 M6 写入 `answer_question` 学情事件，并把答题正误同步给薄弱点模块。
- `answer_question` 固定携带 JSON 布尔型 `isCorrect`、难度、答题耗时、提示次数、`sessionId` 和非空 `kpId`；当前题库没有独立知识点主键，`kpId` 由“学科 + 知识点名称”稳定生成，保证同一知识点可聚合。
- M6 事件时间使用带 `+08:00` 偏移的 ISO 8601 时间。
- M6 或薄弱点模块临时不可用时只记录警告，不会回滚已经保存的主答题记录。
- 前端不需要直接传 `userId`，也不需要再次调用 M6 的用户事件接口，避免越权和重复上报。

### 5.4 获取薄弱知识点练习推荐

```http
GET /api/practice/recommendations?subject=数学&knowledgePoint=导数&count=5
```

`subject`、`knowledgePoint` 可选，`count` 限制为 `1~20`。用户身份由后端从 JWT 获取。返回薄弱点模块的推荐数组，常用字段为 `exerciseId`、`subject`、`knowledgePoint`、`sourceType`、`priority`。

如果当前用户尚无答题薄弱点记录，会正常返回空数组；前端此时不展示推荐标签。

### 5.5 上报练习会话完成

```http
POST /api/practice/sessions/complete
Content-Type: application/json

{
  "sessionId": "sess_1785859200000",
  "questionCount": 10,
  "correctCount": 8,
  "durationSeconds": 300
}
```

该接口以当前 JWT 用户身份向 M6 写入 `finish_practice` 事件，供学情分析、薄弱点判断和后续复习计划使用。`questionCount` 必须在 `1~10000` 之间，`correctCount` 必须在 `0~questionCount` 之间，`durationSeconds` 会限制到 `0~86400`。事件中的 `accuracy` 使用 `0~1` 小数，时间使用带 `+08:00` 偏移的 ISO 8601 时间。

---

## 6. 答题记录与错题本

### 6.1 查询答题记录

```http
GET /api/practice/records?range=week&wrongOnly=true&knowledgePoint=导数定义
```

参数：`range`、`chapter`、`knowledgePoint`、`wrongOnly`。返回答题记录数组，字段参考提交答案中的 `record`。

### 6.2 查询错题本

推荐接口：

```http
GET /api/wrong-questions?status=UNRESOLVED&chapter=导数与微分&knowledgePoint=导数定义
```

兼容接口：

```http
GET /api/practice/mistakes?status=UNRESOLVED
```

两者当前返回相同结构：

```json
[
  {
    "id": 5,
    "questionId": 12,
    "questionTitle": "导数计算",
    "content": "函数 f(x)=x^2 在 x=2 处的导数是多少？",
    "correctAnswer": "B",
    "analysis": "f'(x)=2x，因此 f'(2)=4。",
    "chapter": "导数与微分",
    "knowledgePoint": "导数定义",
    "difficulty": "BASIC",
    "questionType": "SINGLE_CHOICE",
    "subject": "数学",
    "track": "高数期末",
    "status": "UNRESOLVED",
    "wrongCount": 1,
    "reviewCount": 0,
    "doubtful": true,
    "reviewNote": "注意链式法则",
    "lastWrongAt": "2026-08-04T20:30:00",
    "lastReviewAt": null,
    "resolvedAt": null
  }
]
```

> 前端将 `doubtful=true` 显示为“已收藏”。

### 6.3 收藏/取消收藏

```http
PUT /api/wrong-questions/5/focus
Content-Type: application/json

{
  "focused": true
}
```

- `true`：收藏。
- `false`：取消收藏。
- 不传请求体：自动切换当前状态。

成功后返回更新后的错题对象。

### 6.4 修改错题状态和笔记

```http
PATCH /api/practice/mistake-book/5
Content-Type: application/json

{
  "doubtful": true,
  "reviewNote": "注意链式法则",
  "status": "REVIEWING"
}
```

所有字段均可选。`status` 只能为 `UNRESOLVED`、`REVIEWING`、`RESOLVED`。

### 6.5 从答题记录修改错题信息

```http
PATCH /api/practice/mistakes/101
Content-Type: application/json

{
  "doubtful": true,
  "reviewNote": "重新检查公式",
  "status": "REVIEWING"
}
```

路径中的 `101` 是答题记录 `recordId`，不是错题 `id`。接口会同步更新对应错题。

### 6.6 删除错题记录

```http
DELETE /api/wrong-questions/5
```

只删除当前用户的错题本记录，不删除题库题目和历史答题记录。

### 6.7 批量生成错题重做会话

```http
POST /api/wrong-questions/batch-redo
Content-Type: application/json

{
  "ids": [5, 8, 10]
}
```

`ids` 是错题记录 ID；传空数组或不传请求体时，获取当前用户所有未解决错题。

`data` 示例：

```json
{
  "sessionId": "mistake_redo_1785859200000",
  "mode": "MISTAKE_REDO",
  "totalCount": 3,
  "questions": [],
  "mistakeIds": [5, 8, 10]
}
```

---

## 7. 看板与统计

### 7.1 成长看板

```http
GET /api/practice/dashboard
```

主要返回字段：

| 字段 | 说明 |
| --- | --- |
| `totalPoints` | 总积分 |
| `totalAnswers` / `correctAnswers` | 总答题数/正确数 |
| `accuracy` | 总正确率，百分数 |
| `currentStreak` | 当前连续做题天数 |
| `conqueredMistakes` | 已攻克错题数 |
| `leaderboardHidden` | 是否隐藏榜单身份 |
| `preferredTrack` | 当前排行榜方向 |
| `rankTitle` | 积分称号 |
| `dailyTarget` / `todayCount` | 每日目标/今日完成数 |
| `weekly` | 最近一周趋势 |
| `mistakeCount` | 未解决错题数 |
| `checkin` | 签到状态对象 |

### 7.2 细化统计

```http
GET /api/practice/statistics?range=week
```

`data` 主要字段：

```json
{
  "range": "week",
  "totalAnswers": 20,
  "correctAnswers": 15,
  "incorrectAnswers": 5,
  "accuracy": 75.0,
  "previousAccuracy": 70.0,
  "accuracyChange": 5.0,
  "hasPreviousPeriod": true,
  "totalDurationSeconds": 1200,
  "averageDurationSeconds": 60.0,
  "activeDays": 5,
  "bestStreak": 3,
  "currentStreak": 2,
  "knowledgeDistribution": [],
  "chapterDistribution": [],
  "questionTypeDistribution": [],
  "difficultyDistribution": [],
  "wrongReasonDistribution": [],
  "trend": [],
  "weakKnowledgePoints": [],
  "strongKnowledgePoints": [],
  "recommendations": []
}
```

普通分组项结构：

```json
{
  "name": "导数定义",
  "count": 8,
  "correct": 6,
  "accuracy": 75.0,
  "durationSeconds": 480,
  "averageDurationSeconds": 60.0
}
```

知识点掌握度在普通分组项基础上增加：

```json
{
  "recentAccuracy": 80.0,
  "recentTrend": 10.0,
  "sampleConfidence": 100.0,
  "masteryScore": 78.5,
  "masteryLevel": "较熟练",
  "needsReview": false,
  "recommendation": "增加一道进阶题验证迁移能力",
  "lastAnsweredAt": "2026-08-04T20:30:00"
}
```

掌握度判断备注：

- 综合分约为：历史平滑正确率 `55%` + 最近 5 题加权表现 `30%` + 样本可信度 `15%`。
- 样本数少于 3 题时统一为“数据不足”，不会直接判定“已掌握”。
- `>=85` 已掌握，`>=70` 较熟练，`>=55` 待巩固，否则为薄弱。
- 错因属于规则推断，`wrongReasonDistribution[].inferred=true`，不是用户手动选择的结论。

### 7.3 获取 AI 待复习提醒

```http
GET /api/practice/review-reminders
Authorization: Bearer <token>
```

用户身份由后端从 JWT 获取，不接收前端传入的 `userId`。接口返回当天到期及已经逾期、但尚未完成的提醒。

```json
[
  {
    "id": 100,
    "userId": 1,
    "courseId": "PRACTICE-MATH-001",
    "reminderType": "SPACED_REPETITION",
    "content": "导数定义间隔复习",
    "scheduledDate": "2026-08-04",
    "priority": 2,
    "isReviewed": 0,
    "reviewedAt": null
  }
]
```

### 7.4 完成 AI 复习提醒

```http
POST /api/practice/review-reminders/100/complete
Authorization: Bearer <token>
Content-Type: application/json

{
  "notes": "已在做题模块完成本次复习"
}
```

后端会校验提醒归属，不能完成其他用户的提醒。当前 `notes` 为兼容字段，数据库暂未保存该备注。错题状态首次变为 `RESOLVED` 时，还会生成 `M2 / MISTAKE_REVIEWED` 事件供 AI 记忆模块更新复习效果。

趋势项结构：

```json
{
  "date": "2026-08-04",
  "count": 8,
  "accuracy": 75.0,
  "durationSeconds": 420,
  "averageDurationSeconds": 52.5
}
```

---

## 8. 排行榜、签到和隐私

### 8.1 获取排行榜

```http
GET /api/practice/leaderboards?track=高数期末
```

`data` 示例：

```json
{
  "track": "高数期末",
  "hidden": false,
  "trackRanking": [
    {
      "rank": 1,
      "userId": 1,
      "username": "xiaoming1",
      "studentName": "小明1",
      "totalPoints": 120,
      "streakDays": 3,
      "rankTitle": "进阶刷题人"
    }
  ],
  "teamRanking": [],
  "teamHint": "小队表与成员表已建立，可基于 inviteCode 扩展 3-8 人封闭小队。",
  "rankStorage": "MYSQL_FALLBACK"
}
```

> Redis 不可用时自动使用 MySQL 排名；连续天数会按最后做题日期实时失效，不会永久停留在旧数值。

### 8.2 每日签到

```http
POST /api/practice/checkin
```

重复签到不会重复加分：

```json
{
  "checked": true,
  "alreadyChecked": false,
  "points": 2,
  "status": {}
}
```

### 8.3 查询签到状态

```http
GET /api/practice/checkin/status
```

```json
{
  "checkedToday": true,
  "checkinPoints": 2,
  "monthCheckins": 6,
  "streakDays": 3,
  "totalPoints": 122
}
```

### 8.4 更新排行榜隐私

```http
PATCH /api/practice/privacy
Content-Type: application/json

{
  "hidden": true
}
```

---

## 9. 导出与数据清理

### 9.1 导出记录

```http
GET /api/practice/export?wrongOnly=false
```

- `wrongOnly=false`：下载 `practice-records.txt`。
- `wrongOnly=true`：下载 `mistakes.txt`。
- 响应为 `text/plain; charset=UTF-8`，不是统一 JSON。
- 前端下载时必须附带 JWT；不要使用未加请求头的普通 `fetch`。

### 9.2 清空当前用户刷题数据

```http
DELETE /api/practice/records
```

会删除当前用户的：

- 答题记录
- 错题本记录
- 签到记录
- 统计数据

题库题目和其他用户数据不会删除。该操作不可撤销，前端必须二次确认。

---

## 10. 前后端字段映射

前端 `practiceService.js` 会做以下转换：

| 后端值 | 前端值 |
| --- | --- |
| `数学` | `math` |
| `英语` | `english` |
| `计算机` | `computer` |
| `SINGLE_CHOICE` | `single_choice` |
| `MULTIPLE_CHOICE` | `multi_choice` |
| `FILL_BLANK` | `fill_blank` |
| `SHORT_ANSWER` | `short_answer` |
| `BASIC` | 难度星级 1/2 |
| `ADVANCED` | 难度星级 3/4 |
| `HARD` | 难度星级 5 |
| `UNRESOLVED` | `unresolved` |
| `REVIEWING` | `reviewing` |
| `RESOLVED` | `resolved` |

---

## 11. 组员联调注意事项

1. 做题页面请拉取 `frontend`；做题后端可拉取 `backend-M1`，跨模块集成环境使用 `develop`。
2. 所有个人错题、统计、连续天数必须通过 `/api/practice/submit` 写入数据库，前端 Mock 默认关闭。
3. 不要只保存浏览器本地会话；服务重启后可恢复的是数据库中的题目、答题记录、错题和统计。
4. 题库 CRUD 目前只校验“已登录”，尚未增加管理员角色限制；上线前应补权限控制。
5. 普通练习的题量选择目前由前端完成，后端 `/questions` 不接收 `questionCount`。
6. `/questions` 是管理列表，会返回标准答案；学生正式做题优先使用 `/next`，后续建议新增不含答案的批量练习会话接口。
7. `PATCH /mistake-book/{mistakeId}` 使用错题 ID；`PATCH /mistakes/{recordId}` 使用答题记录 ID，注意不要混用。
8. 收藏状态复用错题字段 `doubtful`：`true` 表示收藏，`false` 表示取消收藏。
9. 数据库必须使用本项目最新迁移结构；如果 Flyway 提示同版本迁移文件冲突，先执行干净构建，避免使用旧 `target/classes` 残留文件。
10. 推荐组员先调用登录接口取得 Token，再依次测试：`filters -> questions/next -> submit -> recommendations -> sessions/complete -> wrong-questions -> statistics`。
